import { Component } from '@angular/core';

/** Placeholder for `/` while {@link appEntryGuard} redirects; should never stay visible. */
@Component({
  selector: 'app-root-shell',
  template: '',
  standalone: true,
})
export class AppRootShell {}
